server:
  port: ${PORT:5000}

base-url: /api

spring:
  application:
    name: project-5

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/changelog-master.xml

  jpa:
    hibernate:
      ddl-auto: create-drop